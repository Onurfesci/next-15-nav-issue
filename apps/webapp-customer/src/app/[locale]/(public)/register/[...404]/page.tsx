'use client';

import { Box } from '@aspire/ui';

const NotFoundPage = () => {
  return (
    <Box data-testid="not-found-page" sx={{ mt: 8 }}>
      Not found
    </Box>
  );
};

export default NotFoundPage;
