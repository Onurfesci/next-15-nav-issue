const defaultConfig = {
  awsChatEnabled: true,
  apiUrl: 'https://api.fakeurl.com',
  authUrl: 'https://auth.fakeurl.com/realms/login',
  featureFlags: {
    debitCards: false,
    directDebits: false,
  },
};

export default defaultConfig;
